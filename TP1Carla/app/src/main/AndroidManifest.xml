<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carla.tp1carla">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera"  />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroProductoActivity" />
        <activity android:name=".RegistroVentaActivity" />
        <activity android:name=".ConsultarProductoActivity" />
        <activity android:name=".ConsultarListaProductoActivity" />
        <activity android:name=".ConsultarListaVentaActivity" />
        <activity android:name=".DetalleProductoActivity" />
        <activity android:name=".DetalleVentaActivity" />
        <activity android:name=".MainProductoActivity" />
        <activity android:name=".MainVentaActivity" />
    </application>

</manifest>